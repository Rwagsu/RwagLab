<Page x:Class="RwagLab.Views.Pages.Settings.AboutSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:RwagLab.Views.Pages.Settings"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="24"
                    Orientation="Vertical"
                    Spacing="8">
            <!-- Title -->
            <ImageIcon Width="128"
                       Height="128"
                       HorizontalAlignment="Center"
                       Source="/Assets/Icons/AppIcons/icon_full.png" />
            <TextBlock HorizontalAlignment="Center"
                       FontWeight="Bold"
                       Style="{ThemeResource TitleTextBlockStyle}"
                       Text="{x:Bind GetApplicationName()}" />
            <HyperlinkButton HorizontalAlignment="Center" NavigateUri="{x:Bind GetOwnerLink()}">
                <TextBlock>
                    <Run Text="{x:Bind GetOwner()}" />
                    <Run x:Uid="AboutSettingsPage_Owner" />
                </TextBlock>
            </HyperlinkButton>
        </StackPanel>
    </ScrollViewer>
</Page>
